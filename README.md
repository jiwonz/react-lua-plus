# react-lua-setup
A lune script that setups `react-lua` with some additional features

# Installation
Use git submodule
```sh
git submodule add
```

# Usage
## Setup using wally
```sh
lune run react-lua-setup wally
```
